   var toggle = true;
        $(".purpose").each(function(element){
            if ($(this).val() == '') {
                alertify.alert()
                .setting({
                    'frameless':false,
                    'title':"목표 작성 오류",
                    'closableByDimmer':true,
                    'closable': false,
                    modal: false,
                    transition:'pulse',
                    'pinnable': false,
                    message: '목표는 필수 입력 사항입니다.',
                    'label':'Ok'
                }).show();
                toggle = false; 
            }
            else {
                $(".minute option:selected").each(function(element){
                    var  time = Number($(this).val())*60 + Number($(".minute option:selected").parent().parent().parent().parent().children(".card-body-1-left-minute").children(".second").children("select").children("option:selected").val())
                    if (time == 0) {
                        alertify.alert()
                        .setting({
                            'frameless':false,
                            'title':"예상 시간 선택 오류",
                            'closableByDimmer':true,
                            'closable': false,
                            modal: false,
                            transition:'pulse',
                            'pinnable': false,
                            message: '예상 시간은 필수 선택 사항입니다.',
                            'label':'Ok'
                        }).show();
                        toggle = false; 
                    }
                    else {
                        $(".target").each(function(element){
                            if ($(this).val() == '') {
                                alertify.alert()
                                .setting({
                                    'frameless':false,
                                    'title':"타겟명 작성 오류",
                                    'closableByDimmer':true,
                                    'closable': false,
                                    modal: false,
                                    transition:'pulse',
                                    'pinnable': false,
                                    message: '타겟명은 필수 작성 사항입니다.',
                                    'label':'Ok'
                                }).show();
                                toggle = false; 
                            }
                            else {
                                $(".testernumber option:selected").each(function(element){
                                    if ($(this).val() == '0') {
                                        alertify.alert()
                                        .setting({
                                            'frameless':false,
                                            'title':"테스터수 선택 오류",
                                            'closableByDimmer':true,
                                            'closable': false,
                                            modal: false,
                                            transition:'pulse',
                                            'pinnable': false,
                                            message: '테스터수는 필수 선택 사항입니다.',
                                            'label':'Ok'
                                        }).show();
                                        toggle = false; 
                                    }
                                    else {
                                        console.log("Dd")
                                    }
                                });
                            }
                        });
                    }
                });
            }
        });
    if (toggle == true) {
        document.getElementById("second-form").submit();

    }
   


